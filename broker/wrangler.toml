name = "peachpi-oauth-broker"
main = "src/worker.ts"
compatibility_date = "2025-01-01"

# Short-lived OAuth state + pickup storage. Create with:
#   wrangler kv namespace create OAUTH
# then paste the id below.
[[kv_namespaces]]
binding = "OAUTH"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
