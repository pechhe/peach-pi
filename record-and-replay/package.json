{
  "name": "@peach-pi/record-replay",
  "version": "0.1.0",
  "private": true,
  "description": "Record & Replay — capture a recurring desktop task and synthesize a reusable natural-language skill file for the pi agent.",
  "type": "module",
  "license": "MIT",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "keywords": [
    "pi-package",
    "pi",
    "mcp",
    "model-context-protocol",
    "record-and-replay",
    "skills"
  ],
  "bin": {
    "record-replay": "bin/record-replay.mjs"
  },
  "scripts": {
    "build:native": "bash native/build.sh",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types tests/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  },
  "pi": {
    "note": "This is a standalone stdio MCP server, NOT a pi extension. Register it through pi-mcp-adapter so pi launches it as an MCP tool server. See README.md."
  }
}
